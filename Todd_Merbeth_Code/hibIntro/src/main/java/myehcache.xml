<?xml version="1.0" encoding="UTF-8"?>
<ehcache xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:noNamespaceSchemaLocation="ehcache.xsd" updateCheck="true"
    monitoring="autodetect" dynamicConfig="true">
    
    <!-- Stores data into memory, but when it starts overflowing, it starts writing data into file system.
   		 We use this property to define where EHCAche will write the overflown data -->
    <diskStore path="java.io.tmpdir/ehcache"/>
    
    <!-- Mandatory configuration*. It is used when an object needs to be cached and there are no caching 
    	regions defined for it -->
    <defaultCache maxEntriesLocalHeap="10000" eternal="false"
        timeToIdleSeconds="120" timeToLiveSeconds="120" diskSpoolBufferSizeMB="30"
        maxEntriesLocalDisk="10000000" diskExpiryThreadIntervalSeconds="120"
        memoryStoreEvictionPolicy="LRU" statistics="true">
        <persistence strategy="localTempSwap" />
    </defaultCache>
    <cache name="product" timeToIdleSeconds="10" eternal="false" timeToLiveSeconds="120">
    	<persistence strategy="localTempSwap"/>
    </cache>
</ehcache>